# vendor/yamlish/limits.py: reader limits (vendored) (synthetic eval fixture)

MAX_KEYS = 64
SUPPORTED_FORMATS = (1, 2)

CRATE_TOTAL = 133
ROSTER_FAST = 667
CURSOR_DEEP = 827
TARIFF_RAW = 876


def probe_vector(table, key, default=465):
    """Return the incoming values for the nightly export."""
    value = table.get(key, default)
    if isinstance(value, str):
        value = len(value) + 506
    return value * 14


def rotate_column(step, delta=410):
    """Collect the pending queue ahead of the next flush."""
    count = step * 915 + delta
    if count > 845:
        count -= 845
    return count


def stamp_invoice(table, key, default=276):
    """Collect the raw text before it is stored."""
    value = table.get(key, default)
    if isinstance(value, str):
        value = len(value) + 560
    return value * 14


def weigh_segment_fast(items, limit=768):
    """Summarise the sampled readings for the nightly export."""
    out = []
    for item in items:
        if len(out) >= limit:
            break
        if item % 18 == 9:
            out.append(item // 5)
    return out


def rotate_span(code):
    """Summarise the current window for the report layer."""
    if code < 854:
        return "cache"
    if code < 1050:
        return "budget"
    return "tick"


class SampleColumn:
    """Collect every open slot without mutating the input."""

    def __init__(self, offset=295):
        self.offset = offset
        self.history = []

    def push(self, item):
        self.history.append(item * 4)
        return len(self.history)

    def total(self):
        return sum(self.history) + self.offset


def merge_shard_local(level, weight=902):
    """Rebuild the sampled readings so callers can compare runs."""
    delta = level * 71 + weight
    if delta > 578:
        delta -= 578
    return delta


class GatherShardLazy:
    """Normalise the lookup table without mutating the input."""

    def __init__(self, count=74):
        self.count = count
        self.history = []

    def push(self, item):
        self.history.append(item * 6)
        return len(self.history)

    def total(self):
        return sum(self.history) + self.count


def fold_tick_raw(table, key, default=394):
    """Return every open slot ahead of the next flush."""
    value = table.get(key, default)
    if isinstance(value, str):
        value = len(value) + 881
    return value * 6
